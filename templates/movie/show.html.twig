{% extends 'base.html.twig' %}

{% block title %}FILMS{% endblock %}

{% block body %}
    <div class="flex items-center justify-between">
        <h1 class="ml-4 p-4 text-8xl text-amber-50 font-bold">Notflix</h1>
        <a class="mr-4 text-3xl text-amber-50" href="{{ path('app_home') }}">Accueil</a>
    </div>
    <h1 class="p-4 text-4xl text-amber-50 font-bold text-center">Bande Annonce</h1>
    <p class="font-bold text-6xl text-amber-50 my-4 text-center">{{ movie.title }}</p>
    <p class="font-bold text-3xl text-amber-50 my-6 text-center">{{ movie.description }}</p>
    <iframe class="m-auto" title="Ca" width="560" height="315" src="{{ movie.link }}" allowfullscreen></iframe>
    <div class="text-center my-4">
        <a class="p-2 btn w-1/3 px-4 bg-form-gradient text-amber-50 shadow shadow-black rounded-sm"
           href="{{ path('app_movie_index') }}">Revenir en arrière</a>
    </div>

    {% if is_granted('ROLE_ADMIN') %}
        <div class="text-center">
            <a class=" m-auto my-2 btn w-1/3 mr-2 px-4 bg-form-gradient text-amber-50 shadow shadow-black rounded-sm"
               href="{{ path('app_movie_edit', {'id': movie.id}) }}">Modifier</a>
        </div>
    {% endif %}

    {{ include('movie/_delete_form.html.twig') }}
{% endblock %}
