{{ form_start(form) }}
<div class="mb-2 flex flex-row items-center">
    {{ form_label(form.title, 'Titre', {
        'label_attr': {'class': ''
        }}) }}

    {{ form_widget(form.title, {
        'attr': {
            'class': 'text-black my-2 w-1/2 m-auto rounded-sm'
        }
    }) }}
</div>
<div class="mb-4 flex flex-row items-center">
    {{ form_label(form.description, 'Description', {
        'label_attr': {'class': ''
        }}) }}

    {{ form_widget(form.description, {
        'attr': {
            'class': 'text-black my-2 w-1/2 m-auto rounded-sm'
        }
    }) }}
</div>
<div class="mb-4 flex flex-row items-center">
    {{ form_label(form.link, 'URL', {
        'label_attr': {'class': ''
        }}) }}

    {{ form_widget(form.link, {
        'attr': {
            'class': 'text-black my-2 w-1/2 m-auto rounded-sm'
        }
    }) }}
</div>
<div class="m-auto">
    <button class="my-2 btn mr-2 px-4 bg-form-gradient text-amber-50 shadow shadow-black rounded-sm">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}